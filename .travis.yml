language: node_js
node_js:
  - "6"
  - "7"
  - "8"

services:
  - postgresql

cache:
  directories:
    - "node_modules"

before_install:
  - "npm install -g npm"
before_script:
  - "npm install"
  - "psql -c 'create database queue;' -U test"